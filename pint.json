{
    "preset": "laravel",
    "exclude": [
        "vendor",
        "bootstrap/cache",
        "storage",
        "node_modules",
        "public/build"
    ],
    "rules": {
        "no_unused_imports": true,
        "ordered_imports": {
            "sort_algorithm": "alpha"
        }
    }
}
